#!/usr/bin/env python3

from setuptools import setup

setup(name        = 'arenas',
      version     = '0.1',
      description = 'Library to try and get variables from the system environment, script arguments or interactively with the user',
      author      = ['PidgeyL'],
      packages    = ['arenas'],
)
